/* import React from 'react'
import Adapter from '../setupTests'
import { shallow, mount, render } from 'enzyme'
import App from '../App'
import Contact from '../components/Contact'

import renderer from 'react-test-renderer'/*
// import toJson from 'enzyme-to-json';

/* describe('Check Banner', () => {

  it('render three <Contact/> component', function() {
      let wrapper = shallow(<App />).find(Contact)
      expect(wrapper).to.have.length(7)
  })

  it('should render without throwing an error', function() {
     let contact = shallow(<Contact />).contains( <div className="contact-paragraph"></div>);
    expect(contact).toBe(false);
  });

}) */
